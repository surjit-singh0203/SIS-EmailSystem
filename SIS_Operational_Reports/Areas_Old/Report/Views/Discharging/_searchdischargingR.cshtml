

@model DataBuildingLayer.DischargingReport


<div id="discharging">
    


    <div id="idMaindiv" class="container">
        <div class="row">

            @*<div class="mb-lg-1 row">
                    <div style="margin-left:550px" class="col-sm-3">

                        <input id="idFirst" class="form-control" type="text" />
                        <span id="messageLat" style="color:red"></span>
                    </div>

                    <div class="col-sm-3">

                        <input id="idSecond" name="datefilter" placeholder="yyyy-mm-dd - yyyy-mm-dd" class="form-control" type="text" />
                        <span id="messageLat" style="color:red"></span>
                    </div>


                    <div class="col-md button-row-top">
                        <div class="row">
                            <div class="col">
                                <button id="btnSearch" class="function-button submit-button">

                                    <i class="fa fa-plus-circle mr-2"></i>Search

                                </button>
                            </div>

                        </div>
                    </div>

                </div>*@

            <table class="GeneratedTable">
                <thead>
                    <tr>
                        <th style="display:none">Id</th>
                        <th>Voyage No.</th>
                        <th>Leg Port (A)</th>
                        <th>Port</th>
                        <th>Report Date</th>
                        <th>Status</th>
                        <th>Submit Date</th>
                        <th>Action</th>


                    </tr>

                </thead>
                <tbody>

                    @foreach (var item in Model.DischargingReportList)
                    {


                    <tr>
                        <td style="display:none"> @Html.DisplayFor(modelItem => item.Id)</td>
                        <td> @Html.DisplayFor(modelItem => item.VoyageNumber)</td>
                        <td> @Html.DisplayFor(modelItem => item.LegPort_A)</td>
                        <td> @Html.DisplayFor(modelItem => item.PortName)</td>
                        <td>
                            @*@{var dates = Convert.ToDateTime(item.ReportDateTime).ToString("dd-MM-yyyy HH:mm");
            @Html.DisplayFor(modelItem => dates,"{0:dd-MM-yyyy}")
            }*@
                            @{
                                DateTime? dt = item.ReportDateTime;
                                if (dt != null)
                                {
                                    @Html.Raw(dt.Value.ToString("dd-MMM-yyyy HH:mm"))
                                }
                            }
                        </td>
                        <td>

                            @{ var svd = item.SaveDraft;
                                                if (svd == true)
                                                {
                                @Html.Raw("Draft") }
                                                else
                                                {
                                @Html.Raw("Submit") } }
                        </td>
                        <td>
                            @{ DateTime? cdt = item.CreatedDate;
                                                if (cdt != null)
                                                {
                                @Html.Raw(cdt.Value.ToString("dd-MMM-yyyy")) } }
                        </td>
                        <td>

                            <div class="row button-row">
                                <div class="col table-button-container">
                                    <button type="button" class="function-button table-button">
                                        @Html.ActionLink("View Detail", "Edit", "Discharging", new { id = item.Id, VId = item.VesselId }, new { onclick = "" })
                                    </button>
                                </div>
                              
                            </div>

                        </td>


                    </tr>
                    }


                </tbody>
            </table>
            <!-- Codes by Quackit.com -->
            @*<nav aria-label="Page navigation example">
                    <ul class="pagination">


                        @{
                            int totalRecords = 0;
                            foreach (var item in Model.GetNoonRList)
                            {
                                totalRecords = item.TotalCount;
                                break;
                            }
                            //int totalRecords = Convert.ToInt32(TempData.Peek("TotalRecords"));
                            // totalRecords = Convert.ToInt32(TempData.Peek("TotalRecords"));

                            int pageSize = SIS_Operational_Reports.Common.CommonMethods.PAGESIZE;

                            int tot_pages = totalRecords / pageSize;
                            int remainder = totalRecords % pageSize;

                            tot_pages = remainder > 0 ? (tot_pages + 1) : tot_pages;

                            int currPage = Convert.ToInt32(TempData.Peek("CurrentPage"));

                            if (totalRecords > 0)
                            {
                                if (currPage == 0)
                                {
                                    currPage = 1;
                                }

                                if (currPage != 1)
                                {
                                    <li class="page-item"><a class="page-link"> @Html.ActionLink("First", "_searchnoonR", "DailyNoon", new { pageNo = 1 }, null)</a></li>
                                }

                                if (currPage != tot_pages)
                                {
                                    for (int cnt = currPage; cnt < (currPage + tot_pages); cnt++)
                                    {
                                        if (cnt == currPage)
                                        {
                                            <li class="page-item active"><a class="page-link" href="#">@cnt</a></li>
                                        }
                                        else
                                        {
                                            <li class="page-item">@Html.ActionLink(cnt.ToString(), "_searchnoonR", "DailyNoon", new { pageNo = cnt }, null)</li>
                                        }

                                        if (cnt == tot_pages)
                                        {
                                            break;
                                        }
                                    }
                                }


                                if (currPage == tot_pages)
                                {
                                    <li class="page-item active"><a class="page-link" href="#">@tot_pages</a></li>
                                }
                                else
                                {
                                    <li class="page-item">@Html.ActionLink("Last", "_searchnoonR", "DailyNoon", new { pageNo = tot_pages }, null)</li>
                                }
                            }
                        }
                    </ul>
                </nav>*@

        </div>
    </div>


</div>

<script type="text/javascript">
    $(function () {

        $('input[name="datefilter"]').daterangepicker({
            autoUpdateInput: false,
            locale: {
                cancelLabel: 'Clear'
            }
        });

        $('input[name="datefilter"]').on('apply.daterangepicker', function (ev, picker) {
            $(this).val(picker.startDate.format('YYYY/MM/DD') + ' - ' + picker.endDate.format('YYYY/MM/DD'));
        });

        $('input[name="datefilter"]').on('cancel.daterangepicker', function (ev, picker) {
            $(this).val('');
        });

    });
</script>

<script>

    $("#btnSearch").click(function () {
        //alert(34);

        var firstVal = $("#idFirst").val();
        var secondVal = $("#idSecond").val();




        var dateF = $('#idSecond').data('daterangepicker').startDate.format('YYYY-MM-DD');
        var dateT = $('#idSecond').data('daterangepicker').endDate.format('YYYY-MM-DD');




        $("#berthing").html('');

        $.ajax({
            type: 'GET',
            url: '/Report/Arrival/arrivallist?pageNo=1&firstVal=' + firstVal + '&dateF=' + dateF + '&dateT=' + dateT,
            datatype: 'json',
            cache: false,
            contentType: 'application/json; charset=utf-8',
            processData: false,
            success: function (data) {

                // alert(data);
                /* if (response.Result === true) {*/

                //window.location.reload();

                $("#discharging").html(data);


                //;

                // $("#idMaindiv").html('');

                //$("#idMaindiv").html(data);
                // alert(data);
                //}
                //else {
                //    toastr["error"](response.Message);
                //}
            },
            //failure: function (response) {
            //    toastr["error"](response.Message);
            //}
        });





    });

</script>
